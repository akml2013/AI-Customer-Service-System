spring:
  application:
    name: gateway
  cloud:
    nacos:
      username: nacos
      password: nacos
      config:
        server-addr: 127.0.0.1:8848
        namespace: dev  # 根据启动参数切换
        group: PRODUCT_GROUP
        file-extension: yaml

  config:
    import:
      - optional:nacos:gateway.yaml?group=COMMON_GROUP